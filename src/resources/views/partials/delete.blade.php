

<button type="button"
        data-id="{{$id}}"
        data-label="{{$label}}"
        data-url="{{route($route, $id)}}"
        class="btn btn-outline-danger btn-xs timer-delete-record">
    <i class="fa fa-trash"></i>

</button>
